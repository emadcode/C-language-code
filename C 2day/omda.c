#include <stdio.h>
#include <cs50.h>

int main() {
    int o = 0;

    while (o < 10) {
        printf("o = %d\n", o);
        o++;
    }
    //"Here, the numbers will start from 1 to 9 i."
    
    return 0;
}
